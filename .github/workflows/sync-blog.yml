# DEPRECATED — kept only to avoid breaking links to past runs.
# The active workflow is build-blog.yml. You can safely delete this file.
name: (deprecated) Old Hashnode sync

on:
  workflow_dispatch: # manual only, will not run automatically

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use 'Build blog from Markdown' instead."
